import 'package:flutter/material.dart';

class PumpListPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pump List'),
      ),
      body: ListView(
        children: [
          // List of pumps
        ],
      ),
    );
  }
}
